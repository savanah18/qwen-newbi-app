# Triton-specific dependencies
# These are already included in the Dockerfile, listed for reference

# Inference frameworks
torch>=2.0.0
transformers>=4.57.0
accelerate>=1.12.0
bitsandbytes>=0.49.0
safetensors>=0.7.0
vllm>=0.6.0

# Triton client libraries
tritonclient[all]>=2.44.0

# Image processing
pillow>=12.0.0

# HTTP and utilities
requests>=2.32.0
pydantic>=2.12.0
numpy>=2.4.0

# Optional: For performance monitoring
prometheus-client>=0.19.0
